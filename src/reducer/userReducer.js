import { closeSidebar, openSidebar } from "../context/actioin"

export const initState={
    isSidebar:false
}

export const reducer =(state,action)=>{
    switch(action.type){
        default:
            throw new Error();
    }
}